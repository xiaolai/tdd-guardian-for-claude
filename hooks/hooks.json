{
  "hooks": {
    "PreToolUse": [
      {
        "description": "Guards Bash tool calls; blocks commits that skip test/coverage gates per config",
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/tdd-guardian/pretool_guard.js"
          }
        ]
      }
    ],
    "TaskCompleted": [
      {
        "description": "Runs tests and coverage after task completion; reports gate results",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/tdd-guardian/taskcompleted_gate.js"
          }
        ]
      }
    ]
  }
}
