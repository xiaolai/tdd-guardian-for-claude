{
  "hooks": {
    "PreToolUse": [
      {
        "description": "Guards Bash tool calls; checks that every lane gating a commit or push has a fresh pass",
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/tdd-guardian/pretool_guard.js"
          }
        ]
      }
    ],
    "TaskCompleted": [
      {
        "description": "Runs the taskCompleted lanes after task completion, merges coverage, and records per-lane gate state",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/tdd-guardian/taskcompleted_gate.js"
          }
        ]
      }
    ]
  }
}
